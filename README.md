# 8-bit-ALU
An 8-bit Arithmetic and Logic Unit that takes 8-bit length of input operands and can perform 8 operations on then such as Addition, Subtraction, Multiplication, Left Shift, Right Shift, AND, OR and XOR.
In this we have used verilog language to build the 8-bit ALU
